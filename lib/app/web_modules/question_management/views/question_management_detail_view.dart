import 'package:flutter/material.dart';

class QuestionManagementDetailView extends StatelessWidget {
  const QuestionManagementDetailView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}